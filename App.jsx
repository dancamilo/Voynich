import Home from "@/pages/Home";

/**
 * VOYNICH is a single, fully static informational page. There is no
 * authentication, backend, or multi-page routing to manage, so the app
 * shell is intentionally minimal — it just renders the page.
 */
function App() {
  return <Home />;
}

export default App;
